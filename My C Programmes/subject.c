#include<stdio.h>
int main()
{
    int physics, chemistry, biology, total;
    printf("Enter the value of 3 subjects: ");
    scanf("%d, %d ,%d", &physics, &chemistry, &biology);
    total : physics + chemistry + biology;
    printf ("Total of 3 subjects is:  %d\n", total );
    return 0;

}